<%.
    export Hello with rust::Display
    export Hello with rust::Render

    import in Rust {
        std.name.uppercase as A
    }
.%>
using std.name.uppercase as A in Rust

<% for k, v in term if guard %>
   for c in term if guard
    <% if condition %>
       if conditional
    <% else %>
       else
    <% end %>
       end
<% else %>
   else
<% end %>
   end

<%! end %>
