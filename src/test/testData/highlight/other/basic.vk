<% class std.name.path in Rust %>
   class std.name.path in Rust

<% using std.name.uppercase as A in Rust %>
   using std.name.uppercase as A in Rust

<% for c in term if guard %>
   for c in term if guard
    <% if condition %>
       if conditional
    <% end %>
       end
<% end %>
   end

